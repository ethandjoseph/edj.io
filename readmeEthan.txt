git fetch
//update local repo (which has only main branch)

git checkout <branch_name>

git checkout -b <branch_name>

git add .
//will add everything in source control

git add <file_name>
//will specific file in source control

